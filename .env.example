# Banco de dados PostgreSQL (Vercel Postgres, Supabase, Neon, etc)
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Secret para NextAuth (gere com: openssl rand -base64 32)
AUTH_SECRET="sua-chave-secreta-aqui"

# URL do site (para NextAuth)
NEXTAUTH_URL="https://seu-dominio.vercel.app"

# Lomadee API Configuration
LOMADEE_API_BASE_URL="https://api-beta.lomadee.com.br"
LOMADEE_API_KEY="sua-api-key-lomadee"
LOMADEE_SOURCE_ID="seu-source-id"
